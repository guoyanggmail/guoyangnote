# 2022年面试记录

## 榜样科技(9.13)

1. 做过哪些性能优化;
2. `ijkplayer` 如何进行优化和复用;
3. `PK` 功能和直播做过哪些性能优化;
4. `rtmp` 推拉流有了解过么，有什么优点;
5. `CDN` 转发是什么;
6. `Livedata` 了解么，有什么优缺点;
7. `Databinding` 用过么;
8. `DownTimer` 跳秒的问题;
9. 滑动事件冲突的解决方式;
10. 音频流信息中如何获取音量信息;
11. `requestLayout` 和 `invalidate` 的区别;
12. 自定义 `View` 需要注意哪些事项;
13. 用过哪些设计者模式;
14. `View` 的三种测量模式.

## 燃星科技(9.20)

1. 什么是 `Kotlin` 高阶函数，见过什么常用的高阶函数;
   > 高阶函数：将函数用作一个函数的参数或者返回值的函数。
2. `Kotlin` 静态变量和静态方法怎么定义，伴生对象是什么，`@JvmStatic` 有什么作用，全局变量是静态的么;
3. `let`、`apply`、`run` 的区别，他们的返回值都是什么;
4. 内联函数 `inline`、`noinline`、`crossinline` 的区别;
5. 知道什么是运算符重载么，有用到过运算符重载么;
6. `infix` 是什么，项目中有见过的场景么;
7. `lateinit`、`by lazy` 的区别，`lateinit` 的变量一定不为空么，`by lazy` 背后的原理了解过么;
8. 什么是协程，`Flow` 是什么，`Flow` 用来替代谁的，`Flow` 和 `LiveData` 的区别，`Flow.map` 和 `Flow.flatMap` 的区别;
9. 协程如何处理崩溃异常，`CoroutineExceptionHandler` 用过么;
10. `LiveData` 是什么，`LiveData` 有什么缺点，`Flow` 短时间发送多次会丢失么;
11. 项目有用过 `Jetpack` 相关的库么，`DataBinding` 的 `Adapter` 是做什么用的;
12. 面向对象和面向过程编程区别;
13. `Binder` 机制讲解一下;
14. `App` 的启动流程说一下，`App` 的进程是由谁创建的，`App` 的进程和 `Application` 的对应关系(一对一);
15. 垃圾回收算法有了解过么，是怎么回收的;
16. 性能优化的常用手段说一下;
17. `Bitmap` 内存占用大小怎么计算，`xxhdpi`、`xhdpi` 代表什么;
18. 屏幕适配是怎么做的，了解过今日头条屏幕适配的原理么，看过源码么，除了这种适配方式还有其他适配方案么;
19. `Glide` 的缓存机制了解么，内部实现原理，`LruCache` 的特点，内部使用了什么数据结构;
20. `OkHttp` 的拦截器有什么用，拦截器用了什么设计者模式，它的责任链模式是怎么设计的;
21. `Retrofit` 的数据适配器的原理知道么;
22. 常用的设计者模式说一下，`Kotlin` 的扩展函数使用了什么设计者模式;
23. 解决过最大的崩溃率是什么，怎么解决的;
24. 直播秒开是如何实现的;

## 川谷汇数字科技(9.21)

1. 性能优化做过哪些方面;
2. 什么是内存泄露，`OOM` 是什么，`ANR` 是什么;
3. 进程间通信方式有哪些，用过哪些，`Binder` 机制的原理，它对比其他有什么优势么;

## APUS(9.21)

### 一面

1. 做过哪些性能优化(好像每个公司都问);
2. 第三方库的初始化时长你们是怎么统计的，用过系统自带的性能分析工具么;
3. 页面启动优化是怎么优化的;
4. `App` 启动速度和崩溃率是怎么缩减一半的，`ARouter` 插件做了些什么，编译期记录路由信息记录到哪里了;
5. 你们敏捷开发的工作模式是什么;
6. 基础组件库的搭建需要考虑哪些因素，埋点库都实现了什么功能，如果请求接口后台保存了但是返回超时，本地数据和后台数据重复了怎么处理，设置缓存大小如果重要埋点及时性如何保证;
7. 下载库有用到断点续传么，是如何实现的，每次传给后台的 `length` 是本地记录的 `length` 么(好像 `length` 要往前移);
8. 异步任务处理库是用来做什么的，它是如何衔接下一个任务的;
9. 项目中遇到的难点是什么，直播秒开是怎么优化的;
10. 线程池有了解过么，它的几种类型，`ThreadPoolExecutor` 的构造方法里面参数有哪些，构造 `ThreadPoolExecutor` 的时候核心线程数和最大线程数有什么注意点么(好像核心线程数=CPU数量*2);
11. `App` 的启动流程简单说下，为什么说 `Fragment` 比 `Activity` 的性能好;
12. 子线程创建 `Handler` 需要注意什么，除了使用 `Looper.prepare`、`Looper.loop`，还有更简单的方法么(我属实不知道);
13. 进程间通信方式有哪些，用过哪些，`Binder` 机制的原理(emmm，又问);
14. `sp` 是线程安全的么，为什么(我就知道它是进程不安全的);

### 二面

1. 协程一般的使用场景，使用协程如何进行线程切换;
2. `jetpack` 都用到了哪些库;
3. 线程池 `execute()` 和 `submit()` 方法的区别，单线程线程池怎么创建;
4. 异步串型任务有了解过么;
5. `HandlerThread` 有用过么;
6. `BroadcastReceiver` 了解么，`BroadcastReceiver` 是进程安全的么，静态广播和动态广播哪个优先级高(在相同优先级的情况下，动态注册优先级别高于静态注册);
7. 平时怎么学习，侧重的点是那些方面，遇到问题的解决思路;
8. 崩溃率是怎么解决的;

## 好未来(9.22)

1. `Kotlin` 协程的内部实现原理;
2. `Kotlin` 高阶函数有哪些，简单介绍下扩展函数;
3. 说一说 `ViewGrup` 的事件分发机制;
4. 一个 `XML` 布局是如何绘制到页面的，`View` 的绘制流程;
5. 聊一聊 `Handler` 机制，`Handler` 在子线程可以更新 `UI` 么，如果没有 `checkThread()`，`Handler` 如何在子线程更新 `UI`，需要注意哪些点;
6. `Binder` 机制是什么，进程间通信有哪些方式，`Binder` 机制的优点是什么，如果共享内存还需要 `Binder` 机制么，你用到过常用的进程间通信的方式;
7. `Activity` 横竖屏切换时生命周期执行的顺序，`onSaveInstance()` 在哪个生命周期调用的;
8. 说一说内存模型有哪些，如何判断这个对象是否可以被回收，具体的回收算法有哪些，优缺点是什么;
9. 你常用的性能优化的手段，减少包体积大小是怎么做的，`so` 库的动态下发如果让你设计怎么实现，需要注意些什么;
10. 说一说你觉得比较有挑战的项目，遇到过比较棘手的问题，直播秒开你是怎么实现的;
11. `Netty` 的优势是什么，丢包、粘包怎么处理，`Netty` 底层关于丢包粘包的处理策略;

## 洋葱学院(9.23)

1. 你们的性能优化都做了些什么，什么是内存泄露，如何避免，`LeakCanary` 的源码有看过么，它是怎么检测内存泄露的;
2. `Handler` 持有一个弱引用的 `Activity`，在切换到后台时，这个弱引用会被回收么，说一下四种引用模式的区别;
3. 你们的页面启动速度有做什么特殊的优化么，如果我在 `onResume()` 方法执行逻辑和 `view.post()` 执行逻辑有什么区别;
4. `Handler` 机制大概说下，我在子线程如何使用 `Handler`，我在线程1有个任务A和B，主线程有个任务C，我想让A任务先执行，然后C任务执行，C任务执行完成B任务开始执行怎么做;
5. 你们包体积优化除了常规优化做了哪些，`so` 库动态下发的思路说下，`gradle` 定义一个插件，它运行在什么阶段，`ASM` 运行在什么阶段;
6. 说一下你处理过的事件冲突案例，具体是怎么处理的;
7. 有没有做过自定义 `View`，`requestLayout` 和 `invalidate` 的区别，如果让你写一个钟表的自定义 `View` 你会怎么做;
8. 你们崩溃率是怎么解决的，`Thread.UncaughtExceptionHandler` 不能捕获主线程的异常，主线程的异常你们怎么捕获的(好像这里是面试官错了，`Thread.UncaughtExceptionHandler` 可以捕获主线程的异常);
9. 你们项目的架构是怎样的，简单说一下你们项目 `MVVM` 的业务流程，如果不用 `ARouter`，你们业务 `module` 还可以有哪些通信方式，如果 `module` 直接通过接口通信，有没有实现思路;
10. `OkHttp` 有了解过么，有看过它的拦截器的源码么;
11. `Glide` 有了解过其内部实现原理么，比如说如何监听 `Activity` 的生命周期，`Glide` 的内部缓存是怎么实现的;
12. 二叉树的层序遍历;

## 小米-系统开发(9.26)

1. 你项目中遇到过印象深刻的难点，是怎么解决的;
2. `can't deliver broadcast`具体定位崩溃原因，看过源码么，除了在全局的`Thread.UncaughtExceptionHandler`进行拦截过滤，你们后期有定位到具体问题么;
3. 广播发送大的图片如何处理，为什么`Bundle`的大小限制是1MB，`LiveData`没有限制数据大小么;
4. `Handler`中的`Looper`的作用，线程间`Handler`是具体如何通信的，线程的`sleep`和`Handler`中的`epoll`区别，`epoll`的原理有了解过么;
5. `Looper`可以多次创建么，为什么，那如果反射清除`ThreadLocal`中的`Looper`，可以再次创建么;
6. 协程有了解过吗，其实现原理是什么，为什么 `Google` 推荐使用协程而不是线程，使用协程和使用线程池有什么区别么;
7. 事件分发的流程说下，事件冲突是怎么处理的，`Down` 事件如果被父布局拦截消费，为什么子 `View` 就收不到其他事件了，除了在 `ViewGroup` 中拦截和子 `View` 中拦截还有其他方式么;
8. `Dagger` 注解和 `Retrofit` 的注解有什么区别，源码看过么;
9. 算法：打印 `module` 的所有依赖;

## 花椒直播

1. `Kotlin` 扩展函数了解么，写一个自定义的扩展函数;
2. 什么是 `Kotlin` 高阶函数，写一个参数是函数(接受两个 `Int` 类型，返回 `String` 类型)的函数;
3. `Kotlin` 的协程是什么，对比线程有什么优势，它内部原理是如何实现的，为什么说它比线程更轻量级;
4. 协程是如何做到挂起的，`supend` 函数的作用是什么，协程的调度器有几种类型，手写一个两个网络同步请求的协程;
5. 线程池了解么，构造方法里面的几个参数，协程是如何基于线程池做封装的，如果携程挂起一个线程，会阻塞当前线程么;
6. `Handler` 消息机制介绍一下，子线程创建 `Handler` 需要注意些什么;
7. 事件分发机制有了解么，事件拦截如何进行处理;
8. `App` 直播列表页面怎么做(有 `Banner`，有标签，有直播封面列表)，`RecyclerView` 中多布局怎么定义，`LayoutManager` 用什么，需要注意些什么;
9. `1v1` 匹配功能的时序图画一些，整体流程说一下;
10. 项目中有处理过印象比较深刻的问题;
11. `Http` 和 `Https` 的区别，请求返回的 `code` 码 `500` 是什么意思;

## 滴滴

### 一面

1. `View` 的绘制流程简单说下，为什么不能在子线程进行 `View` 的绘制;
2. 事件分发机制和事件拦截机制大概聊一下，如果我在 `ViewGroup` 中拦截了 `Down` 事件，其他事件还会分发子 `View` 么，为什么;
3. 界面为什么会卡顿，如何解决界面卡顿问题，如果我有大量子线程，处理大量事物，导致 `CPU` 运行很慢，界面会卡顿么;
4. 为什么要分为内核空间和用户空间，进程间通信的方式，`Binder`的原理，`AIDL`用过么，简单说下怎么用;
5. `Handler` 机制说一下，`sendDelayMessage` 是如何保证任务的延迟执行的，它的消息是在处理的时候才添加的么，`Handler` 是调用什么方法进行消息阻塞的，`epoll` 的原理有了解过么;
6. `Looper` 中的 `Printer` 有了解过么，它有什么作用(主线程卡顿的监控);
7. `ANR` 有了解过么，你们项目是如何处理 `ANR` 的;
8. 简单说下动画的几种类型，说下他们的优缺点，`ObjectAnimator` 和 `ValueAnimator` 谁是谁的子类;
9. `LiveData` 有什么特性，有什么缺点，它是如何监听生命周期的;
10. `ViewModel` 有了解过么，它内部是如何保证 `Activity`、`Fragment` 配置改变还能保存数据的;
11. 泛型有了解过么，`Java` 中的 `extent`、`super` 和 `Kotlin` 中的 `out`、`in` 简单介绍下，他们有什么区别么;
12. 类的加载机制了解么，为什么要使用双亲委托机制;
13. `Java` 的内存泄露是什么引起的，如何避免和解决，有什么排查的方法么，`Profile` 分析工具用过么;
14. `JVM` 的内存模型简单介绍下;
15. `volatile` 简单的说下，双重校验锁单例模式为什么要在锁之前和锁之后进行两次判空;
16. 线程池了解过么，简单说下如何创建线程池，线程池的几种类型，线程池的核心参数，核心线程可以被回收么，线程池内部工作流程;
17. 什么是 `Kotlin` 的高阶函数，内联函数是什么;
18. 协程的原理，`Flow` 的冷流、热流;
19. `Java` 和 `Kotlin` 之间如何进行代码调用，需要注意些什么点;
20. `Https` 和 `Http` 的区别，`Https` 的证书校验流程，说一下接口 `AES`、`RSA` 加密流程;
21. 算法：设计一个栈，用 `O1` 的时间复杂度获取栈的最小值;

### 二面

1. 你们项目的整体架构设计;
2. 包体积是如何优化的，`so` 库的动态下发如何设计;
3. 海外 `App` 包的多国家的差异化怎么处理;
4. 埋点库的设计与具体的实现思路;
5. 线程池有了解过么，你们项目中有哪些业务场景使用了线程池;
6. 如何避免内存泄漏;
7. 挑一个你写的业务功能点说说实现方案;
8. 算法：荷兰旗;

## 牵手未来(10.20)

1. 整个项目的负责的模块，架构，用了什么设计者模式;
2. 协程的实现原理，`RxJava` 了解过么，对比 `Flow` 的区别;
3. `Room` 了解过么，对比其他数据库的好处，它内部怎么配合 `LiveData` 实现的;
4. `RxJava` 的 `map、flatMap` 的区别;
5. 探探首页的左滑右滑你怎么实现;
6. `Https` 证书的认证过程;
7. 签名证书 `v1、v2、v3` 的区别;

## 陌陌(10.21)

1. `View` 的绘制流程，有哪几种测量模式，比如 `ViewGroup` 是 `MATCH_PARENT`，里面有一个 `WRAP_CONTENT` 的子 `View`，子 `View` 显示多大;
2. 知道哪些进程间通信的实现方式，`Binder` 的优点;
3. `Handler` 的 `sendMessage` 和 `sendMessageDelayed` 的方法区别，内部是如何存放消息的，它的 `poll` 机制是怎么实现的，内存屏障的实现原理;
4. 事件分发机制聊一下，如果子 `View` 的 `onTouchEvent` 消费了事件，`ViewGroup` 的 `dispatchTouchEvent` 方法是怎么处理的;
5. 设计题：一个 `RecyclerView` 和 `ImageView`，`RecyclerView` 下拉放大图片、上滑缩小图片，缩小到正常比例时开始滑动列表，你会怎么实现;
6. `Fresco` 的缓存是如何实现的，`LruCache` 和 `DisCache` 有什么区别，内部使用了什么数据结构，`LinkedHashMap` 它是如何实现最近最少使用排序的;
7. `App` 的启动流程说一下，从点击桌面图标到页面可见的过程;
8. `Android` 中的动画有哪几种，你们的语音房麦位 `1` 给其他麦位送礼的效果怎么实现，除了 `ValueAnimator` 还有其他实现方案么;
9. 内存泄露有了解过么，为何会内存泄露，单例类如何避免持有 `Activity` 的 `Context`，除了替换 `Application` 的 `Context` 还有其他方案么;
10. `Java` 的四种引用类型，具体介绍下它们的区别;
11. `Java` 的内存模型有哪些，垃圾回收算法了解过么，什么可以作为 `GCRoot` 的根结点，老年代的标记清除算法会导致内存碎片，如果内存中没有足够的连续内存空间，如何放置一张 `Bitmap` 不会导致 `OOM`;
12. `String str = "123"` 和 `String str = new String("123")`，他们 `==` 是什么结果，`equals` 是什么结果，为什么;
13. 什么条件下会发生死锁，如何在线程中保证数据的安全性，`volatile` 和 `synchronized` 的区别，`synchronized` 和 `CAS` 的区别，`synchronized` 有哪几种用法，区别是什么;
14. 线程池有了解过么，它的构造方法中有哪些参数，如果它的核心线程数、等待队列、非核心线程数都满了，再进来一个任务会怎么样，线程池有哪几种类型，如果想要执行延迟任务用哪个类型;
15. `HashMap` 的数据结构介绍一下，它内部是如何 `put`、`get` 的，它是怎样扩容的，默认的大小是多少;
16. 协程的内部实现原理;
17. 你们是如何保证直播的稳定率的，弱网情况下你们怎么处理;
18. 你做过哪些性能优化;
19. 项目负责的模块，实现 `IM` 的技术栈(应用层协议、通信层协议的选型)，为何选型 `Netty`，消息的到达率如何保证;
20. 你们项目使用了组件化么，项目整体结构是怎样分层的，业务组件之间相互的功能调用怎么处理;
21. `ARouter` 内部的实现原理了解过么，如何实现组件间通信的;
22. 礼物播放库你们是自己写的么，为何切换到腾讯的 `PAG`，`PAG` 内部是如何解决视频不透明的问题的;
23. 你们数据库使用的什么，Room存了些什么信息，聊天详情你们是存了一张表么，如果数据量大的话，会很耗时，你怎么优化，除了子线程(因为子线程也耗时);
24. 聊一聊你最近看过的第三方源码;
